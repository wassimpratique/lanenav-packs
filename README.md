# LaneNav — packs de cartes hors ligne

Données © contributeurs OpenStreetMap, licence ODbL (https://www.openstreetmap.org/copyright), extraits Geofabrik.
Format : `{zone}-{roads|world}.bin` = tuiles z13 gzippées concaténées, index dans `{zone}-{roads|world}.json`, liste dans `packs.json`.
